# Full Data Deletion Status

The initial skeleton stores no Dev Recall user data. A later Privacy & Data task must document platform-specific application-data locations, preview and confirmation, shutdown of owned tasks, transactional database deletion where applicable, removal of logs/configuration/backups, secure-store key cleanup, failure recovery, and an explicit guarantee that project source files are never deleted.
