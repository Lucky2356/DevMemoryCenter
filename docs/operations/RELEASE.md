# Release Process Status

No release process is approved or implemented. Do not publish artifacts or enable auto-update. Before the first release, document reproducible builds, supported platform CI, least-privilege workflows, SBOM, dependency/license review, artifact signing, Windows code signing, key protection/rotation, integrity verification, downgrade protection, revocation, installer testing, recovery, and release approval.
